import VIEW from "./view";

const mockJobPosts = VIEW.JOB_POSTS;
const mockUserProfiles = VIEW.USER_PROFILES;
const mockNotifications = VIEW.NOTIFICATIONS;
const mockInterviews = VIEW.INTERVIEWS;
const mockCandidates = VIEW.CANDIDATES;

export {
  mockCandidates,
  mockInterviews,
  mockJobPosts,
  mockNotifications,
  mockUserProfiles,
};
